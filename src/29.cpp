#include <iostream>

int main() {
    int num1 = 5;
    int num2 = 3;

    if (num1 > num2) {
        std::cout << "Num1 is greater than Num2." << std::endl;
    } else {
        std::cout << "Num1 is not greater than Num2." << std::endl;
    }

    return 0;
}
